server:
  port: 9002

spring:
  application:
    name: owner-api

  profiles:
    active: local

geolocation:
  kakao:
    client-id: ${OAUTH2_KAKAO_CLIENT_ID}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui/index.html

openapi:
  service:
    url: http://localhost:8080/owner-api
