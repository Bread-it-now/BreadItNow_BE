spring:
  config:
    activate:
      on-profile: local
    import: application-mysql-local.yml

  rabbitmq:
    host: localhost
    port: 5672
    username: root
    password: root
    
rabbitmq:
  queue.name: sample.queue
  exchange.name: sample.exchange
  routing.key: sample.key
